import { Fragment } from "react/jsx-runtime"
import { NavBar } from "./shared/NavBar"

export const Header = () => {
  return (
    <Fragment>
        <header>
            <NavBar/>
        </header>
    </Fragment>
  )
}